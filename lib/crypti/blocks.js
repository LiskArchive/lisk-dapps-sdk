/*
	Crypti blocks API calls
 */

var sandbox = null;

function Blocks(sandbox) {
	sandbox = sandbox;
}

Blocks.prototype.get = function (id) {
	// get block by id
}

Blocks.prototype.getFullBlock = function (id) {
	// get block by id include transactions
}

Blocks.prototype

module.exports = Blocks;